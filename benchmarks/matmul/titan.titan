function matmul(A: {{integer}}, B:{{integer}}): {{integer}}
    local C : {{integer}} = {}
    for i = 1, #A do
        local line: {integer} = {}
        for j = 1, #B[i] do
            local cij = 0
            for k = 1, #A[i] do
                cij = cij + A[i][k] * B[k][j]
            end
            line[j] = cij
        end
        C[i] = line
    end
    return C
end
