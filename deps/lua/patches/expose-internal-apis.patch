diff -ruN upstream/Makefile upstream-patched/Makefile
--- upstream/Makefile	2025-06-26 13:04:37.000000000 +0000
+++ upstream-patched/Makefile	2026-08-02 10:32:13.228760635 +0000
@@ -40,7 +40,7 @@
 
 # What to install.
 TO_BIN= lua luac
-TO_INC= lua.h luaconf.h lualib.h lauxlib.h lua.hpp
+TO_INC= lua.h luaconf.h lualib.h lauxlib.h lua.hpp luacore.h
 TO_LIB= liblua.a
 TO_MAN= lua.1 luac.1
 
diff -ruN upstream/src/Makefile upstream-patched/src/Makefile
--- upstream/src/Makefile	2025-07-07 00:19:14.000000000 +0000
+++ upstream-patched/src/Makefile	2026-08-02 10:35:24.788930287 +0000
@@ -50,7 +50,7 @@
 # Targets start here.
 default: $(PLAT)
 
-all:	$(ALL_T)
+all:	$(ALL_T) luacore.h
 
 o:	$(ALL_O)
 
@@ -70,7 +70,7 @@
 	./$(LUA_T) -v
 
 clean:
-	$(RM) $(ALL_T) $(ALL_O)
+	$(RM) $(ALL_T) $(ALL_O) luacore.h
 
 depend:
 	@$(CC) $(CFLAGS) -MM l*.c
@@ -226,4 +226,9 @@
 lzio.o: lzio.c lprefix.h lua.h luaconf.h lapi.h llimits.h lstate.h \
  lobject.h ltm.h lzio.h lmem.h
 
+# The prerequisites must in the same order that we want in the output.
+CORE_HEADERS=llimits.h lctype.h lobject.h lopcodes.h lmem.h lobject.h lzio.h ltable.h lfunc.h ltm.h lparser.h llex.h lundump.h lstate.h lcode.h ldebug.h lapi.h ldo.h lgc.h lvm.h lstring.h
+luacore.h: gen-lua-core-h.awk $(CORE_HEADERS)
+	awk -f gen-lua-core-h.awk $(CORE_HEADERS) > $@
+
 # (end of Makefile)
diff -ruN upstream/src/lauxlib.c upstream-patched/src/lauxlib.c
--- upstream/src/lauxlib.c	2025-12-15 10:44:39.000000000 +0000
+++ upstream-patched/src/lauxlib.c	2026-08-02 10:32:13.229528200 +0000
@@ -1200,3 +1200,11 @@
                   (LUAI_UACNUMBER)ver, (LUAI_UACNUMBER)v);
 }
 
+LUALIB_API void luaL_checkcoreversion_ (lua_State *L, lua_Number ver, size_t sz) {
+  lua_Number v = LUA_VERSION_RELEASE_NUM;
+  if (sz != LUAL_NUMSIZES)  /* check numeric types */
+    luaL_error(L, "core and library have incompatible numeric types");
+  else if (v != ver)
+    luaL_error(L, "version mismatch: app. needs %f, Lua core provides %f",
+                  (LUAI_UACNUMBER)ver, (LUAI_UACNUMBER)v);
+}
diff -ruN upstream/src/lauxlib.h upstream-patched/src/lauxlib.h
--- upstream/src/lauxlib.h	2025-12-15 10:44:39.000000000 +0000
+++ upstream-patched/src/lauxlib.h	2026-08-02 10:32:13.229836446 +0000
@@ -47,6 +47,10 @@
 #define luaL_checkversion(L)  \
 	  luaL_checkversion_(L, LUA_VERSION_NUM, LUAL_NUMSIZES)
 
+LUALIB_API void (luaL_checkcoreversion_) (lua_State *L, lua_Number ver, size_t sz);
+#define luaL_checkcoreversion(L)  \
+    luaL_checkcoreversion_(L, LUA_VERSION_RELEASE_NUM, LUAL_NUMSIZES)
+
 LUALIB_API int (luaL_getmetafield) (lua_State *L, int obj, const char *e);
 LUALIB_API int (luaL_callmeta) (lua_State *L, int obj, const char *e);
 LUALIB_API const char *(luaL_tolstring) (lua_State *L, int idx, size_t *len);
diff -ruN upstream/src/llimits.h upstream-patched/src/llimits.h
--- upstream/src/llimits.h	2025-12-15 10:44:39.000000000 +0000
+++ upstream-patched/src/llimits.h	2026-08-02 10:32:13.230034117 +0000
@@ -312,12 +312,7 @@
 */
 #if !defined(LUAI_FUNC)
 
-#if defined(__GNUC__) && ((__GNUC__*100 + __GNUC_MINOR__) >= 302) && \
-    (defined(__ELF__) || defined(__MACH__))
-#define LUAI_FUNC	__attribute__((visibility("internal"))) extern
-#else
-#define LUAI_FUNC	extern
-#endif
+#define LUAI_FUNC  LUA_API
 
 #define LUAI_DDEC(dec)	LUAI_FUNC dec
 #define LUAI_DDEF	/* empty */
diff -ruN upstream/src/lua.h upstream-patched/src/lua.h
--- upstream/src/lua.h	2025-12-15 10:44:40.000000000 +0000
+++ upstream-patched/src/lua.h	2026-08-02 10:32:13.230194007 +0000
@@ -517,7 +517,7 @@
 #define LUA_VERSION_RELEASE	LUAI_TOSTR(LUA_VERSION_RELEASE_N)
 
 #define LUA_VERSION	"Lua " LUA_VERSION_MAJOR "." LUA_VERSION_MINOR
-#define LUA_RELEASE	LUA_VERSION "." LUA_VERSION_RELEASE
+#define LUA_RELEASE	LUA_VERSION "." LUA_VERSION_RELEASE " with core API"
 
 
 /******************************************************************************
diff -ruN upstream/src/lvm.c upstream-patched/src/lvm.c
--- upstream/src/lvm.c	2025-12-15 10:44:40.000000000 +0000
+++ upstream-patched/src/lvm.c	2026-08-02 10:32:13.230479843 +0000
@@ -1969,4 +1969,8 @@
   }
 }
 
+int luaV_strcmp(const TString *ls, const TString *rs) {
+    return l_strcmp(ls, rs);
+}
+
 /* }================================================================== */
diff -ruN upstream/src/lvm.h upstream-patched/src/lvm.h
--- upstream/src/lvm.h	2025-12-15 10:44:40.000000000 +0000
+++ upstream-patched/src/lvm.h	2026-08-02 10:32:13.230906610 +0000
@@ -133,4 +133,6 @@
 LUAI_FUNC lua_Integer luaV_shiftl (lua_Integer x, lua_Integer y);
 LUAI_FUNC void luaV_objlen (lua_State *L, StkId ra, const TValue *rb);
 
+LUAI_FUNC int luaV_strcmp(const TString *ls, const TString *rs);
+
 #endif
